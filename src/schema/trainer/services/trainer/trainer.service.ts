export class TranierService {

    createTrainer(attrs) {
        // const user = new User();
        // user.firstName = attrs.firstName;
        // user.lastName = attrs.lastName;
        // user.email = attrs.email;
        // user.password = attrs.password
        // user.username = attrs.username;
        // user.lang = attrs.lang;
        // return user;
    }
}